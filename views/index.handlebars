<div class="jumbotron">
  <h1 class="display-4">Bem-vindo ao Blog do Node!</h1>
  <p class="lead">Este é um Blog simples, desenvolvido em Nod.js + Express + MongoDB.</p>
  <hr class="my-4">
  <p>Aqui você vai poder ler as novidades dessa tecnologia.</p>
  <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
</div>

<hr>

<h2>Postagens recentes</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small>
        <small>Data de publicação: {{data}}</small>
        </div>
</div>
{{else}}
    <h5>Nenhuma postagem</h5>
{{/each}}